doctype html
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css")
        title Document
    body.bg-light
        .container 
            .row.justify-content-center
                .col-md-6.col-lg-4 
                    .card.mt-5.shadow 
                        .card-body 
                            h3.text-center.mb-4 Inicar Sesion
                            form(action="/login", method="post")
                                .mb-3
                                    label.form-label(for='email') Correo Electronico 
                                    input#email.form-control(type='email', name='email', required, placeholder='usuario@gmail.com')
                                .mb-3
                                    label.form-label(for='password') Contraseña
                                    input#password.form-control(type='password', name='password', required, placeholder='Ingrese su contraseña')
                                button.btn.btn-primary.w-100(type='submit') Iniciar Sesion
                                .text-center.mt-3
                                    a(href="/registrar") ¿No tienes cuenta? Registrate
                            .mt-4.text-muted.text-center
                                small &copy; 2025 Artesanos.com
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js")                   